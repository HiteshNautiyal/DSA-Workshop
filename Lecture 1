Ques 1: print the patterns:-
            *
           **
          ***
         ****
Ans:
#include <iostream>
using namespace std;
int main() 
{
    int n;
    cin>>n;
    
    for(int i=1;i<=n;i++)
    {
      for(int sp=1;sp<=n-i;sp++)
      cout<<" ";
      
      for(int j=1;j<=i;j++)
      cout<<"*";
      
      cout<<endl;
    }
    return 0;
}
--------------------------------------------------------------------------------------------------------------------------
Ques 2: A
        BC
        DEF
        GHIJ
Ans:
#include <iostream>
using namespace std;

int main() 
{
    int n;
    cin>>n;
    
    
    char ch = 'A';
    for(int i=0;i<n;i++)
    {
      
      for(int j=0;j<=i;j++)
      {              
        cout<<ch;
        ch = ch+1;
      }
      cout<<endl;
    }
    return 0;
}
-----------------------------------------------------------------------------------------------------------------------
Ques 3: Total sum of all elements in an array?
Ques 4: find the max and min elements in an array?
Ques 5: Reverse a string?
Ques 6: Total number of words in a string?
#include <iostream>
#include <string>
#include<sstream>
using namespace std;

int main() 
{
    string s;
    getline(cin,s);
    
    string token;
    
    stringstream test(s);
    
    while(getline(test,token,' '))
    {
      cout<<token<<endl;
    }
    return 0;
}
------------------------------------------------------------------------------------------------------------------
Ques 7: Transpose of a matrix?
Ques 8: Linear search 
Ques 9: Fibonacci problem using loops
Ques 10: Climb stairs using loops
Ques 11: House robber leetcode using loops
Ans:

----------------------------------------------------------------------------------------------------------------------
Ques 12: Reaarange a string GFG?
Ans:
class Solution
{
  public:
    string arrangeString(string str)
    {
        string res="";
        int sum=0;
        
        for(int i=0;i<str.length();i++)
        {
            if(str[i]>='A' and str[i]<='Z')
            res.push_back(str[i]);
            else
            {
                int x = str[i]-'0';
                sum += x;
            }
        }
        
        sort(res.begin(), res.end());
       if(sum>0)//agr sum==0 means uss string mei koi arithmetic character nhi hai
       {
       string s1 = to_string(sum);//convert integer to string and also store in a string datatype
       for(auto x: s1)
       {
           res.push_back(x);
       }
       }
        return res;
        
    }
};
------------------------------------------------------------------------------------------------------------------------
qUES 13: Search in 2d matrix?
#include <iostream>
#include <string>
#include<sstream>
using namespace std;

bool search(int arr[3][3], int row, int col, int x)
{
  for(int i=1;i<=row;i++)
   {
     for(int j=1;j<=col;j++)
     {
       if(arr[i][j]==x)
       return 1;
     }
     
   }
   return 0;
}

int main() 
{
   int arr[3][3] = {{1,3,4},{2,5,6},{8,9,0}};
   
   int x = 7;
   
   bool res = search(arr,3,3,x);
   
   if(res == true)
   cout<<"Found";
   else
   cout<<"Not";
    return 0;
}













